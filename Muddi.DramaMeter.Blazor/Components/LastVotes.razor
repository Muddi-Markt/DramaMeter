@if (Positions is not null && Positions.Any())
{
    @foreach (var pos in Positions)
    {
        <circle class="vote-dot" cx="@pos.X" cy="@pos.Y" r="4" />
    }
}

@code {
    [Parameter] public List<(double X, double Y)>? Positions { get; set; }
}
